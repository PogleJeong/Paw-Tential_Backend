<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mul.cam.a.market.dao.MarketDao">

	<select id="searchMarketList" resultType="mul.cam.a.market.dto.MarketDto" parameterType="String">
		select *
		from market
		where statement=#{statement} and category=#{category}
		<if test="">
		</if>
	</select>
	
	<select id="printAllMarketList" resultType="mul.cam.a.market.dto.MarketDto">
		select *
		from market
		order by wdate
		limit 16 offset 0
		<!-- offset #{PAGE} -->
	</select>
	
</mapper>